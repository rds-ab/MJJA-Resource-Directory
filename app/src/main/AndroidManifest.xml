<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.juvenilejustice">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mjja"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mjja_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JuvenileJustice.NoActionBar">
        <activity
            android:name=".ResourceList"
            android:exported="true"
            android:label="@string/title_activity_resource_list"
            android:theme="@style/Theme.JuvenileJustice.NoActionBar.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>